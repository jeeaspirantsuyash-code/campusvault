# campusvault
CampusVault — a full-stack registrar ERP for managing student records. Includes a live dashboard, searchable student directory, and a security-PIN-protected backup/restore system. Pure Node.js backend (built-in SQLite, no npm packages) with a vanilla HTML/CSS/JS frontend.
